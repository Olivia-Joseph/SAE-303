      let selects = document.querySelectorAll(".select");
      const option = {
        root: null,
        rootMargin: "0px 0px 0px 0px",
        threshold: 0.8,
      };

      const observer = new IntersectionObserver(function (entries, observer) {
        entries.forEach((entry) => {
          console.log(entry.isIntersecting);
          if (entry.isIntersecting) {
            entry.target.play();
          } else {
            entry.target.stop();
          }
        });
      }, option);

      selects.forEach((select) => {
        observer.observe(select);
      });

           <lottie-player
              class="select"
              id="meduse"
              src="./arbrev2.json"
              background="transparent"
              loop
            ></lottie-player>

      LottieInteractivity.create({
        player: "#page2",
        mode: "scroll",
        loop: false,
        actions: [
          {
            visibility: [0.1, 0.2],
            type: "play",
          },
        ],
      });






 <section class="consequences">
        <h2 class="consequences__title">
          ... et les conséquences ?
        </h2>

        <lottie-player 
        class="consequences__video" 
        id="firstLottie" 
        src="./json/terre2.json" 
        background="transparent" 
        speed="1" direction="1" 
        mode="normal" 
        hover>
        </lottie-player>
      </section>









.consequences{
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color: #436987;
  width: 100vw;
  height: 100vh;
  padding: 0;
}





            <div class="flip-container">
              <div class="flipper">
                <div class="front">
                  Avant
                </div>
                <div class="back">
                  Arrière
                </div>
              </div>
            </div>
.consequences__title{
  font-family: 'Amaranth', sans-serif;
  font-size: 5rem;
  font-weight: 700;
  padding: 0;
  margin-top: 2rem ;
  text-align: center;
}





.flip-container {
  perspective: 1000px;
  width: 30em;
  height: 30em;
  position: relative;
}

.flip-container:hover .flipper {
  transform: rotateY(180deg);
}

.flipper {
  transition: transform 0.6s;
  transform-style: preserve-3d;
  width: 100%;
  height: 100%;
  position: absolute;
  transform-origin: center;
}

.front,
.back {
  width: 100%;
  height: 100%;
  position: absolute;
  backface-visibility: hidden;
}

.front {
  background-color: #3498db;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  color: white;
}

.back {
  background-color: #e74c3c;
  transform: rotateY(180deg);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  color: white;
}
